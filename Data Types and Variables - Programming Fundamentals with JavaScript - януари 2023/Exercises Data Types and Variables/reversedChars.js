function reversedChars(first, second, last ) {

    let reversedString = `${last} ${second} ${first}`;
    console.log(reversedString);
}

reversedChars('A', 'B', 'C');
reversedChars('1', 'L', '&');
reversedChars('K', '*', 'M');